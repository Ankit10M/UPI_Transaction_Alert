package com.upivoicealert.ui.theme

import androidx.compose.ui.graphics.Color

// ─── ShoutPay brand palette (extracted from the Figma design files) ───────────

/** Primary brand deep indigo — used for CTAs, the voice control, active states. */
val ShoutPayIndigo = Color(0xFF000666)
val ShoutPayIndigoDark = Color(0xFF000440)
val OnIndigo = Color(0xFFFFFFFF)

/** Light indigo tint for containers / selected chips. */
val IndigoContainer = Color(0xFFE3E5FA)
val OnIndigoContainer = Color(0xFF10104A)

/** Secondary accent blue (status highlights, links, secondary CTAs). */
val AccentBlue = Color(0xFF288AE2)
val AccentBlueContainer = Color(0xFFD6ECFF)
val OnAccentBlueContainer = Color(0xFF0A3A66)

/** Electric blue — premium fintech accent for glows, gradients and CTA highlights (dark UI). */
val ElectricBlue = Color(0xFF2E6BFF)
val ElectricBlueBright = Color(0xFF5E93FF)
val ElectricBlueDeep = Color(0xFF1B4FD8)

/** Warm peach tertiary accent (hero illustration, subtle highlights). */
val PeachAccent = Color(0xFFE1A46E)
val PeachContainer = Color(0xFFFFE9D6)
val OnPeachContainer = Color(0xFF5A2E0A)

/** Success green — received amounts, "announced" chips, listening state (#006E1C from app_design). */
val SuccessGreen = Color(0xFF006E1C)
val SuccessGreenLight = Color(0xFFD7F5D9)
val OnSuccessGreenLight = Color(0xFF0B3D16)

/** Warning / attention. */
val WarningAmber = Color(0xFFB26A00)
val WarningAmberLight = Color(0xFFFFE7C4)
val OnWarningAmberLight = Color(0xFF4A2C00)

/** Error. */
val ErrorRed = Color(0xFFBA1A1A)
val ErrorRedLight = Color(0xFFFDE4E4)
val OnErrorRedLight = Color(0xFF5C0A0A)

// ─── Neutrals (light theme) ───────────────────────────────────────────────────

val AppBackgroundLight = Color(0xFFF8F9FA)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFEFF0F4)
val OnSurfaceLight = Color(0xFF1D1E21)
val OnSurfaceVariantLight = Color(0xFF9CA3AF)
val OutlineLight = Color(0xFFE4E6E9)
val OutlineStrongLight = Color(0xFFB9BEC9)
val DividerLight = Color(0xFFECEDF1)

// ─── Neutrals (dark theme) ────────────────────────────────────────────────────

val AppBackgroundDark = Color(0xFF0E0F16)
val SurfaceDark = Color(0xFF171821)
val SurfaceVariantDark = Color(0xFF23242F)
val OnSurfaceDark = Color(0xFFE8E9EE)
val OnSurfaceVariantDark = Color(0xFFB3B6C2)
val OutlineDark = Color(0xFF3A3B48)
val OutlineStrongDark = Color(0xFF5A5C6C)
val DividerDark = Color(0xFF2C2D39)
